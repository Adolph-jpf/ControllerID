我们需要一个脚本，或者网页或者APP，用于Mapping wafer 当中的每个die 的测试结果，并生成一个mapping 文件，用于后续的分析。
每一个DIE的WXY 和测试的结果都存在于，CSV的文件中；
其中有个9列 stlot	、teststep	、asic	、wafer、	X	、Y	、HB、	SB	、ASICID
其中 asic是指的不同的wafer 批次编号 wafer是指ASIC 批次中的wafer片数，
X、Y 是DIE的坐标，HB、SB 是DIE的测试结果，ASICID 是DIE的编号。
提供一个功能，能Mapping出整片wafer的测试结果；
1.首先我提供的csv 文件中，wafer的每个位置都最少有一个DIE的数据；
2.需要根据这些X,Y 的坐标，先画出整个wafer的坐标图，
3.有了wafer上X,Y 的坐标，就能根据DIE的编号X,Y 的坐标，找到对应的HB、SB 的测试结果，则可以Mapping出整片wafer的测试结果；
4.测试结果显示公式如下：每个X,Y的坐标上有一个DIE的记录为1，有第二个就为2，以此往上加读取完所有就可以得到每个X，Y的DIE的总数记录为Location-total；
5.另外记录一个list,每个X,Y的坐标上有一个DIE的记录且SB那列的数字为C000计数为1，第二个就为2，以此往上加读取完所有就可以得到每个X，Y上SB为C000的DIE的总数记录为Failure-total；
6.匹配每个位置的Failure-total/Location-total 则可以得到每个X,Y 位置的Rate, 若Location-total=0，则Rate=0；
7.最后将每个位置的Rate可以输出到Excel的对应表格中，X,Y 为坐标，Rate为值；
8.或者自己画图,区分Rate的值的大小，用不同的颜色表示，大的偏红色，小的偏绿色，中间的偏黄色；
可以提供一个网页，或者APP，或者脚本，用于Mapping出整片wafer的测试结果；我希望能得到最好的效果；






有几个问题：
1. C000的DIE的数据完全没有显示在mapping里面；
2. Mapping图显示的时候，没有显示完整的XY左边，只显示了当前wafer有的XY，而我需要的是完整的XY坐标；
3. Mapping显示的每个XY的原点太大了，重叠一起看不清，建议换成格子类似于EXCEl方块格子；




1.有一些wafer的坐标还是不完整，我提供了单片wafer完整的XY坐标，只有2列X和Y可以作为完整的wafer的基本XY参考；
2.除了分开不一样的ASIC 和wafer外，我希望有个可以把所有ASIC 和wafer 合在一起的Mapping图，





mapping的颜色区分十分不明显：
1. 有DIE的XY坐标，但是failure rate 为0，显示为灰色；
2. 没有DIE的XY坐标，显示为白色，看不出来；
3. 有DIE的XY坐标，且failure rate 不为0，显示为渐变红色；从浅红色到深红色；
整体实现效果类似于excel的color scales

Token: YOUR_GITHUB_TOKEN_HERE